name: default
rules:
  - allow(rw, ${WORK_DIR})
  - allow(rw, ${TMP_DIR})
  - deny(rw, ~/.*)
  - allow(r, /)
---
name: test
rules:
  - allow(rw, ${WORK_DIR})
  - allow(rw, ${TMP_DIR})
  - deny(rw, ~/.*)
  - allow(r, /)

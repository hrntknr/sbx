name: default
rules:
  - allow(rw, ${WORK_DIR})
  - allow(rw, ${TMP_DIR})
  - deny(rw, ~/.*)
  - allow(r, /)
---
name: k8s
k8s: true
rules:
  - allow(rw, ${WORK_DIR})
  - allow(rw, ${TMP_DIR})
  - allow(r, ~/.kube/kuberc)
  - deny(rw, ~/.*)
  - allow(r, /)
